@model IEnumerable<SistemaInventario2017.Models.TipoEmpaque>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Descripcion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Descripcion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CodigoTipoEmpaque }) |
            @Html.ActionLink("Details", "Details", new { id=item.CodigoTipoEmpaque }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CodigoTipoEmpaque })
        </td>
    </tr>
}

</table>
