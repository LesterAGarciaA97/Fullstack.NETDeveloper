@model IEnumerable<SistemaInventario2017.Models.Compras>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Proveedor.Nit)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Fecha)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Proveedor.Nit)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Fecha)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.NumeroDocumento }) |
            @Html.ActionLink("Details", "Details", new { id=item.NumeroDocumento }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.NumeroDocumento }) |
            @Html.ActionLink("Detalle","VerDetalleCompra","DetalleCompras", new { id = item.NumeroDocumento },null)
        </td>
    </tr>
}

</table>
