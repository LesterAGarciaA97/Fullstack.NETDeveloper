@model IEnumerable<MVCproductos.Models.TipoEmpaque>

@{
    ViewBag.Title = "Index";
}

<h2>Tipo de empaques</h2>

<p>
    @Html.ActionLink("Agregar", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Descripcion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Descripcion)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.CodigoTipoEmpaque }) |
            @Html.ActionLink("Detalles", "Details", new { id=item.CodigoTipoEmpaque }) |
            @Html.ActionLink("Eliminar", "Delete", new { id=item.CodigoTipoEmpaque })
        </td>
    </tr>
}

</table>
